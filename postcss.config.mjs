/** @type {import('postcss-load-config').Config} */
// Tailwind v4 est géré par @tailwindcss/vite — pas besoin de plugin PostCSS
const config = {
  plugins: {
    autoprefixer: {},
  },
}

export default config
